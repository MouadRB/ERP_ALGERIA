{
  "name": "ferza-erp",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev":      "pnpm --parallel --filter './apps/*' run dev",
    "build":    "pnpm --parallel --filter './apps/*' run build",
    "test":     "pnpm --parallel run test",
    "lint":     "pnpm --parallel run lint",
    "bff":      "pnpm --filter bff run dev",
    "frontend": "pnpm --filter frontend run dev"
  },
  "devDependencies": { "pnpm": "^9.0.0" }
}
