import { Card, CardContent, Typography } from "@mui/material";

export default function CatalogueDetailHeader() {
  return (
    <Card variant="outlined">
      <CardContent>
        <Typography variant="body2">CatalogueDetailHeader</Typography>
      </CardContent>
    </Card>
  );
}
