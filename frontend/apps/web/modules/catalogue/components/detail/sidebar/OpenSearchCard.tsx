import { Card, CardContent, Typography } from "@mui/material";

export default function OpenSearchCard() {
  return (
    <Card variant="outlined">
      <CardContent>
        <Typography variant="body2">OpenSearchCard</Typography>
      </CardContent>
    </Card>
  );
}
