import { Card, CardContent, Typography } from "@mui/material";

export default function PerformanceCard() {
  return (
    <Card variant="outlined">
      <CardContent>
        <Typography variant="body2">PerformanceCard</Typography>
      </CardContent>
    </Card>
  );
}
