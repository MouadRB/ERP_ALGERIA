import { Card, CardContent, Typography } from "@mui/material";

export default function StockRapideCard() {
  return (
    <Card variant="outlined">
      <CardContent>
        <Typography variant="body2">StockRapideCard</Typography>
      </CardContent>
    </Card>
  );
}
