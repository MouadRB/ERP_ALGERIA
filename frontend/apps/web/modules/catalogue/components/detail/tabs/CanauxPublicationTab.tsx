import { Card, CardContent, Typography } from "@mui/material";

export default function CanauxPublicationTab() {
  return (
    <Card variant="outlined">
      <CardContent>
        <Typography variant="body2">CanauxPublicationTab</Typography>
      </CardContent>
    </Card>
  );
}
