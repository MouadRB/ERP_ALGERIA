import { Card, CardContent, Typography } from "@mui/material";

export default function HistoriqueTab() {
  return (
    <Card variant="outlined">
      <CardContent>
        <Typography variant="body2">HistoriqueTab</Typography>
      </CardContent>
    </Card>
  );
}
