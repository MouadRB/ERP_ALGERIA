import { Card, CardContent, Typography } from "@mui/material";

export default function PlanificationTab() {
  return (
    <Card variant="outlined">
      <CardContent>
        <Typography variant="body2">PlanificationTab</Typography>
      </CardContent>
    </Card>
  );
}
