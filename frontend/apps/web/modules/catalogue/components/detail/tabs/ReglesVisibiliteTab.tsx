import { Card, CardContent, Typography } from "@mui/material";

export default function ReglesVisibiliteTab() {
  return (
    <Card variant="outlined">
      <CardContent>
        <Typography variant="body2">ReglesVisibiliteTab</Typography>
      </CardContent>
    </Card>
  );
}
