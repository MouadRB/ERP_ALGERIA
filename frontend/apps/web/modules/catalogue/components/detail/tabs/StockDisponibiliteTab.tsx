import { Card, CardContent, Typography } from "@mui/material";

export default function StockDisponibiliteTab() {
  return (
    <Card variant="outlined">
      <CardContent>
        <Typography variant="body2">StockDisponibiliteTab</Typography>
      </CardContent>
    </Card>
  );
}
