import { Card, CardContent, Typography } from "@mui/material";

export default function BulkActionBar() {
  return (
    <Card variant="outlined">
      <CardContent>
        <Typography variant="body2">BulkActionBar</Typography>
      </CardContent>
    </Card>
  );
}
