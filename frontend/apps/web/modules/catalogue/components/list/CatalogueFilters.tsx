import { Card, CardContent, Typography } from "@mui/material";

export default function CatalogueFilters() {
  return (
    <Card variant="outlined">
      <CardContent>
        <Typography variant="body2">CatalogueFilters</Typography>
      </CardContent>
    </Card>
  );
}
