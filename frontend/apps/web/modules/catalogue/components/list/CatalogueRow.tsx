import { Card, CardContent, Typography } from "@mui/material";

export default function CatalogueRow() {
  return (
    <Card variant="outlined">
      <CardContent>
        <Typography variant="body2">CatalogueRow</Typography>
      </CardContent>
    </Card>
  );
}
