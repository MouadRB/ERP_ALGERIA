import { Card, CardContent, Typography } from "@mui/material";

export default function CatalogueTable() {
  return (
    <Card variant="outlined">
      <CardContent>
        <Typography variant="body2">CatalogueTable</Typography>
      </CardContent>
    </Card>
  );
}
