import { Card, CardContent, Typography } from "@mui/material";

export default function ActionsRapidesCard() {
  return (
    <Card variant="outlined">
      <CardContent>
        <Typography variant="body2">ActionsRapidesCard</Typography>
      </CardContent>
    </Card>
  );
}
