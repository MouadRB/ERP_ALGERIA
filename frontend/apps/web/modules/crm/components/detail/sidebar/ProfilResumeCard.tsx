import { Card, CardContent, Typography } from "@mui/material";

export default function ProfilResumeCard() {
  return (
    <Card variant="outlined">
      <CardContent>
        <Typography variant="body2">ProfilResumeCard</Typography>
      </CardContent>
    </Card>
  );
}
