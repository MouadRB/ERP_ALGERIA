import { Card, CardContent, Typography } from "@mui/material";

export default function HistoriqueCommandesTab() {
  return (
    <Card variant="outlined">
      <CardContent>
        <Typography variant="body2">HistoriqueCommandesTab</Typography>
      </CardContent>
    </Card>
  );
}
