import { Card, CardContent, Typography } from "@mui/material";

export default function InteractionsNotesTab() {
  return (
    <Card variant="outlined">
      <CardContent>
        <Typography variant="body2">InteractionsNotesTab</Typography>
      </CardContent>
    </Card>
  );
}
