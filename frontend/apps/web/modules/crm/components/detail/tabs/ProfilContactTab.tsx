import { Card, CardContent, Typography } from "@mui/material";

export default function ProfilContactTab() {
  return (
    <Card variant="outlined">
      <CardContent>
        <Typography variant="body2">ProfilContactTab</Typography>
      </CardContent>
    </Card>
  );
}
