import { Card, CardContent, Typography } from "@mui/material";

export default function RisqueFraudeTab() {
  return (
    <Card variant="outlined">
      <CardContent>
        <Typography variant="body2">RisqueFraudeTab</Typography>
      </CardContent>
    </Card>
  );
}
