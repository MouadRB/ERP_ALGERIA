import { Card, CardContent, Typography } from "@mui/material";

export default function TicketsSupportTab() {
  return (
    <Card variant="outlined">
      <CardContent>
        <Typography variant="body2">TicketsSupportTab</Typography>
      </CardContent>
    </Card>
  );
}
