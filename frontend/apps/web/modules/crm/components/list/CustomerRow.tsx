import { Card, CardContent, Typography } from "@mui/material";

export default function CustomerRow() {
  return (
    <Card variant="outlined">
      <CardContent>
        <Typography variant="body2">CustomerRow</Typography>
      </CardContent>
    </Card>
  );
}
