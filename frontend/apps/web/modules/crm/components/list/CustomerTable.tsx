import { Card, CardContent, Typography } from "@mui/material";

export default function CustomerTable() {
  return (
    <Card variant="outlined">
      <CardContent>
        <Typography variant="body2">CustomerTable</Typography>
      </CardContent>
    </Card>
  );
}
