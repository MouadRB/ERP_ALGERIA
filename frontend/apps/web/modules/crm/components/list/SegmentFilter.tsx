import { Card, CardContent, Typography } from "@mui/material";

export default function SegmentFilter() {
  return (
    <Card variant="outlined">
      <CardContent>
        <Typography variant="body2">SegmentFilter</Typography>
      </CardContent>
    </Card>
  );
}
