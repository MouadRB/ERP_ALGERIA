import { Card, CardContent, Typography } from "@mui/material";

export default function AlertsPanel() {
  return (
    <Card variant="outlined">
      <CardContent>
        <Typography variant="body2">AlertsPanel</Typography>
      </CardContent>
    </Card>
  );
}
