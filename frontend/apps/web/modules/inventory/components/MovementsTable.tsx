import { Card, CardContent, Typography } from "@mui/material";

export default function MovementsTable() {
  return (
    <Card variant="outlined">
      <CardContent>
        <Typography variant="body2">MovementsTable</Typography>
      </CardContent>
    </Card>
  );
}
