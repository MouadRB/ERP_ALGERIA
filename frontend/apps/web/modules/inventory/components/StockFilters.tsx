import { Card, CardContent, Typography } from "@mui/material";

export default function StockFilters() {
  return (
    <Card variant="outlined">
      <CardContent>
        <Typography variant="body2">StockFilters</Typography>
      </CardContent>
    </Card>
  );
}
