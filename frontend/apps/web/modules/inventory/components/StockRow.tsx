import { Card, CardContent, Typography } from "@mui/material";

export default function StockRow() {
  return (
    <Card variant="outlined">
      <CardContent>
        <Typography variant="body2">StockRow</Typography>
      </CardContent>
    </Card>
  );
}
