import { Card, CardContent, Typography } from "@mui/material";

export default function StockTable() {
  return (
    <Card variant="outlined">
      <CardContent>
        <Typography variant="body2">StockTable</Typography>
      </CardContent>
    </Card>
  );
}
