import { Card, CardContent, Typography } from "@mui/material";

export default function SKUDetailHeader() {
  return (
    <Card variant="outlined">
      <CardContent>
        <Typography variant="body2">SKUDetailHeader</Typography>
      </CardContent>
    </Card>
  );
}
