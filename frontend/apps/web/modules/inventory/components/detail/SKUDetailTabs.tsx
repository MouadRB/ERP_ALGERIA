import { Card, CardContent, Typography } from "@mui/material";

export default function SKUDetailTabs() {
  return (
    <Card variant="outlined">
      <CardContent>
        <Typography variant="body2">SKUDetailTabs</Typography>
      </CardContent>
    </Card>
  );
}
