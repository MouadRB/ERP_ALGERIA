import { Card, CardContent, Typography } from "@mui/material";

export default function AlertCard() {
  return (
    <Card variant="outlined">
      <CardContent>
        <Typography variant="body2">AlertCard</Typography>
      </CardContent>
    </Card>
  );
}
