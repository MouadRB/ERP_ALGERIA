import { Card, CardContent, Typography } from "@mui/material";

export default function ReservationCard() {
  return (
    <Card variant="outlined">
      <CardContent>
        <Typography variant="body2">ReservationCard</Typography>
      </CardContent>
    </Card>
  );
}
