import { Card, CardContent, Typography } from "@mui/material";

export default function StockResumeCard() {
  return (
    <Card variant="outlined">
      <CardContent>
        <Typography variant="body2">StockResumeCard</Typography>
      </CardContent>
    </Card>
  );
}
