import { Card, CardContent, Typography } from "@mui/material";

export default function FifoLayersTab() {
  return (
    <Card variant="outlined">
      <CardContent>
        <Typography variant="body2">FifoLayersTab</Typography>
      </CardContent>
    </Card>
  );
}
