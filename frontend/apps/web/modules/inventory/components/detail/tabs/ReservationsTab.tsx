import { Card, CardContent, Typography } from "@mui/material";

export default function ReservationsTab() {
  return (
    <Card variant="outlined">
      <CardContent>
        <Typography variant="body2">ReservationsTab</Typography>
      </CardContent>
    </Card>
  );
}
