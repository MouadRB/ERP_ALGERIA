import { Card, CardContent, Typography } from "@mui/material";

export default function StockNiveauxTab() {
  return (
    <Card variant="outlined">
      <CardContent>
        <Typography variant="body2">StockNiveauxTab</Typography>
      </CardContent>
    </Card>
  );
}
