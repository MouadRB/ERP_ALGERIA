import { Card, CardContent, Typography } from "@mui/material";

export default function OrderDetailHeader() {
  return (
    <Card variant="outlined">
      <CardContent>
        <Typography variant="body2">OrderDetailHeader</Typography>
      </CardContent>
    </Card>
  );
}
