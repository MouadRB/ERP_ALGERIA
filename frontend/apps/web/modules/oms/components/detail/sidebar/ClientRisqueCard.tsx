import { Card, CardContent, Typography } from "@mui/material";

export default function ClientRisqueCard() {
  return (
    <Card variant="outlined">
      <CardContent>
        <Typography variant="body2">ClientRisqueCard</Typography>
      </CardContent>
    </Card>
  );
}
