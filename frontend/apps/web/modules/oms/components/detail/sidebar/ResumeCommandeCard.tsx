import { Card, CardContent, Typography } from "@mui/material";

export default function ResumeCommandeCard() {
  return (
    <Card variant="outlined">
      <CardContent>
        <Typography variant="body2">ResumeCommandeCard</Typography>
      </CardContent>
    </Card>
  );
}
