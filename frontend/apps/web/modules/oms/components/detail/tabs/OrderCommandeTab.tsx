import { Card, CardContent, Typography } from "@mui/material";

export default function OrderCommandeTab() {
  return (
    <Card variant="outlined">
      <CardContent>
        <Typography variant="body2">OrderCommandeTab</Typography>
      </CardContent>
    </Card>
  );
}
