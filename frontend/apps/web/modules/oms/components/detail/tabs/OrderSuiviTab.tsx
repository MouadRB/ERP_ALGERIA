import { Card, CardContent, Typography } from "@mui/material";

export default function OrderSuiviTab() {
  return (
    <Card variant="outlined">
      <CardContent>
        <Typography variant="body2">OrderSuiviTab</Typography>
      </CardContent>
    </Card>
  );
}
