import { Card, CardContent, Typography } from "@mui/material";

export default function QueueRow() {
  return (
    <Card variant="outlined">
      <CardContent>
        <Typography variant="body2">QueueRow</Typography>
      </CardContent>
    </Card>
  );
}
