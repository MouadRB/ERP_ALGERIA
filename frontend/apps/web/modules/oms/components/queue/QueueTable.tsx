import { Card, CardContent, Typography } from "@mui/material";

export default function QueueTable() {
  return (
    <Card variant="outlined">
      <CardContent>
        <Typography variant="body2">QueueTable</Typography>
      </CardContent>
    </Card>
  );
}
