import { Card, CardContent, Typography } from "@mui/material";

export default function UrgencyBanner() {
  return (
    <Card variant="outlined">
      <CardContent>
        <Typography variant="body2">UrgencyBanner</Typography>
      </CardContent>
    </Card>
  );
}
