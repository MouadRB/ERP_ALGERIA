import { Card, CardContent, Typography } from "@mui/material";

export default function ProductDetailHeader() {
  return (
    <Card variant="outlined">
      <CardContent>
        <Typography variant="body2">ProductDetailHeader</Typography>
      </CardContent>
    </Card>
  );
}
