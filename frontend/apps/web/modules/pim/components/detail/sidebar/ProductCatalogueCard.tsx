import { Card, CardContent, Typography } from "@mui/material";

export default function ProductCatalogueCard() {
  return (
    <Card variant="outlined">
      <CardContent>
        <Typography variant="body2">ProductCatalogueCard</Typography>
      </CardContent>
    </Card>
  );
}
