import { Card, CardContent, Typography } from "@mui/material";

export default function ProductStatusCard() {
  return (
    <Card variant="outlined">
      <CardContent>
        <Typography variant="body2">ProductStatusCard</Typography>
      </CardContent>
    </Card>
  );
}
