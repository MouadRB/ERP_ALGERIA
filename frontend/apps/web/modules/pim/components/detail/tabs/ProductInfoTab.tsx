import { Card, CardContent, Typography } from "@mui/material";

export default function ProductInfoTab() {
  return (
    <Card variant="outlined">
      <CardContent>
        <Typography variant="body2">ProductInfoTab</Typography>
      </CardContent>
    </Card>
  );
}
