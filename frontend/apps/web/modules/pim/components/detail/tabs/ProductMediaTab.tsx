import { Card, CardContent, Typography } from "@mui/material";

export default function ProductMediaTab() {
  return (
    <Card variant="outlined">
      <CardContent>
        <Typography variant="body2">ProductMediaTab</Typography>
      </CardContent>
    </Card>
  );
}
