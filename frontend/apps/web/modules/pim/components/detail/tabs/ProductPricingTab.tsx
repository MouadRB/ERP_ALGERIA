import { Card, CardContent, Typography } from "@mui/material";

export default function ProductPricingTab() {
  return (
    <Card variant="outlined">
      <CardContent>
        <Typography variant="body2">ProductPricingTab</Typography>
      </CardContent>
    </Card>
  );
}
