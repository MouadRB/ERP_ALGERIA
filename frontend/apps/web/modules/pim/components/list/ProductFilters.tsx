import { Card, CardContent, Typography } from "@mui/material";

export default function ProductFilters() {
  return (
    <Card variant="outlined">
      <CardContent>
        <Typography variant="body2">ProductFilters</Typography>
      </CardContent>
    </Card>
  );
}
