import { Card, CardContent, Typography } from "@mui/material";

export default function ProductRow() {
  return (
    <Card variant="outlined">
      <CardContent>
        <Typography variant="body2">ProductRow</Typography>
      </CardContent>
    </Card>
  );
}
