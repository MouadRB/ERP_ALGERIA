import { Card, CardContent, Typography } from "@mui/material";

export default function ProductSearch() {
  return (
    <Card variant="outlined">
      <CardContent>
        <Typography variant="body2">ProductSearch</Typography>
      </CardContent>
    </Card>
  );
}
