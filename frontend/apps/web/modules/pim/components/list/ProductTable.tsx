import { Card, CardContent, Typography } from "@mui/material";

export default function ProductTable() {
  return (
    <Card variant="outlined">
      <CardContent>
        <Typography variant="body2">ProductTable</Typography>
      </CardContent>
    </Card>
  );
}
