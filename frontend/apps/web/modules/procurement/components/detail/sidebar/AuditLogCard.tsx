import { Card, CardContent, Typography } from "@mui/material";

export default function AuditLogCard() {
  return (
    <Card variant="outlined">
      <CardContent>
        <Typography variant="body2">AuditLogCard</Typography>
      </CardContent>
    </Card>
  );
}
