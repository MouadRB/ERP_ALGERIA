import { Card, CardContent, Typography } from "@mui/material";

export default function ImpactInventaireCard() {
  return (
    <Card variant="outlined">
      <CardContent>
        <Typography variant="body2">ImpactInventaireCard</Typography>
      </CardContent>
    </Card>
  );
}
