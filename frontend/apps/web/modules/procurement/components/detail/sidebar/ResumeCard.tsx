import { Card, CardContent, Typography } from "@mui/material";

export default function ResumeCard() {
  return (
    <Card variant="outlined">
      <CardContent>
        <Typography variant="body2">ResumeCard</Typography>
      </CardContent>
    </Card>
  );
}
