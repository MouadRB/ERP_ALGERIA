import { Card, CardContent, Typography } from "@mui/material";

export default function ArticlesMontantsTab() {
  return (
    <Card variant="outlined">
      <CardContent>
        <Typography variant="body2">ArticlesMontantsTab</Typography>
      </CardContent>
    </Card>
  );
}
