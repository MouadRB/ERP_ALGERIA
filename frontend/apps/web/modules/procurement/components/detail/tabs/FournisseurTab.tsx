import { Card, CardContent, Typography } from "@mui/material";

export default function FournisseurTab() {
  return (
    <Card variant="outlined">
      <CardContent>
        <Typography variant="body2">FournisseurTab</Typography>
      </CardContent>
    </Card>
  );
}
