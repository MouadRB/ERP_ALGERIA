import { Card, CardContent, Typography } from "@mui/material";

export default function SoDApprobationTab() {
  return (
    <Card variant="outlined">
      <CardContent>
        <Typography variant="body2">SoDApprobationTab</Typography>
      </CardContent>
    </Card>
  );
}
