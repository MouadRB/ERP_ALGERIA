import { Card, CardContent, Typography } from "@mui/material";

export default function AlertsTab() {
  return (
    <Card variant="outlined">
      <CardContent>
        <Typography variant="body2">AlertsTab</Typography>
      </CardContent>
    </Card>
  );
}
