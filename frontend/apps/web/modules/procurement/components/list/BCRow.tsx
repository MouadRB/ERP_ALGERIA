import { Card, CardContent, Typography } from "@mui/material";

export default function BCRow() {
  return (
    <Card variant="outlined">
      <CardContent>
        <Typography variant="body2">BCRow</Typography>
      </CardContent>
    </Card>
  );
}
