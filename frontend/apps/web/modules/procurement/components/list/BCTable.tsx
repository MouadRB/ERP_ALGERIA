import { Card, CardContent, Typography } from "@mui/material";

export default function BCTable() {
  return (
    <Card variant="outlined">
      <CardContent>
        <Typography variant="body2">BCTable</Typography>
      </CardContent>
    </Card>
  );
}
