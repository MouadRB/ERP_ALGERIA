import { Card, CardContent, Typography } from "@mui/material";

export default function SupplierCard() {
  return (
    <Card variant="outlined">
      <CardContent>
        <Typography variant="body2">SupplierCard</Typography>
      </CardContent>
    </Card>
  );
}
