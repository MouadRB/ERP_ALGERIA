import { Card, CardContent, Typography } from "@mui/material";

export default function GlobalControlsBar() {
  return (
    <Card variant="outlined">
      <CardContent>
        <Typography variant="body2">GlobalControlsBar</Typography>
      </CardContent>
    </Card>
  );
}
