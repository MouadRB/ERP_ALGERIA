import { Card, CardContent, Typography } from "@mui/material";

export default function AlertesActiveCard() {
  return (
    <Card variant="outlined">
      <CardContent>
        <Typography variant="body2">AlertesActiveCard</Typography>
      </CardContent>
    </Card>
  );
}
