import { Card, CardContent, Typography } from "@mui/material";

export default function KPIMasterBar() {
  return (
    <Card variant="outlined">
      <CardContent>
        <Typography variant="body2">KPIMasterBar</Typography>
      </CardContent>
    </Card>
  );
}
