import { Card, CardContent, Typography } from "@mui/material";

export default function ResumerExecutifCard() {
  return (
    <Card variant="outlined">
      <CardContent>
        <Typography variant="body2">ResumerExecutifCard</Typography>
      </CardContent>
    </Card>
  );
}
