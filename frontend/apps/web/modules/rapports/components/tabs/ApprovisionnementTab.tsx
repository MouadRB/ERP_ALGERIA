import { Card, CardContent, Typography } from "@mui/material";

export default function ApprovisionnementTab() {
  return (
    <Card variant="outlined">
      <CardContent>
        <Typography variant="body2">ApprovisionnementTab</Typography>
      </CardContent>
    </Card>
  );
}
