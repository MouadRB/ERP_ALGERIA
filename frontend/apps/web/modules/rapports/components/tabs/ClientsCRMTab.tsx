import { Card, CardContent, Typography } from "@mui/material";

export default function ClientsCRMTab() {
  return (
    <Card variant="outlined">
      <CardContent>
        <Typography variant="body2">ClientsCRMTab</Typography>
      </CardContent>
    </Card>
  );
}
