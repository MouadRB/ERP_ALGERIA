import { Card, CardContent, Typography } from "@mui/material";

export default function OverviewTab() {
  return (
    <Card variant="outlined">
      <CardContent>
        <Typography variant="body2">OverviewTab</Typography>
      </CardContent>
    </Card>
  );
}
